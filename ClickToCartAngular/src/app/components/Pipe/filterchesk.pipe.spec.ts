import { FiltercheskPipe } from './filterchesk.pipe';

describe('FiltercheskPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercheskPipe();
    expect(pipe).toBeTruthy();
  });
});
